import React from "react";
import ContactForm from "@/components/contact/ContactForm";

const ContactFormSection = () => {
  return (
    <div className="reveal animate-slide-left w-full">
      <ContactForm />
    </div>
  );
};

export default ContactFormSection;
